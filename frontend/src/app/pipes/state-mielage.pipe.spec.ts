import { StateMielagePipe } from './state-mielage.pipe';

describe('StateMielagePipe', () => {
  it('create an instance', () => {
    const pipe = new StateMielagePipe();
    expect(pipe).toBeTruthy();
  });
});
