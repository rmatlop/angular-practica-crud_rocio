import { CarButtonDirective } from './car-button.directive';

describe('CarButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new CarButtonDirective();
    expect(directive).toBeTruthy();
  });
});
